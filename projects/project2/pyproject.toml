[project]
name = "project2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "jax",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
